# Run `pip3 install -r requirements.txt` to install packages.
# Library dependencies for the main python code.

Scrapy==2.6.1

# Library dependencies for the jupyter notebook

pandas==0.24.2
numpy==1.16.4
